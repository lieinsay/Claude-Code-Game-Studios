# Smoke Check Report

**Date**: 2026-05-15
**Sprint**: UI / HUD / Chart Interface (#16)
**Engine**: Godot 4.6.2 .NET + C#
**QA Plan**: `production/qa/qa-plan-ui-hud-interface-2026-05-15.md`
**Argument**: sprint
**Smoke Checklist**: `tests/smoke/critical-paths.md`

---

## Environment

- Test directory: Found (`tests/`)
- CI configured: Yes (`.github/workflows/tests.yml` runs `dotnet build` and C# test runners)
- Engine: Godot 4.6.2 .NET
- Smoke tests: Found (`tests/smoke/critical-paths.md`, `tests/smoke/session_shell_visual_probe.gd`, `tests/smoke/session_shell_perf_probe.gd`)
- Local Godot binary used for runtime probe: `D:\Program Files (x86)\Godot_v4.6.2-stable_mono_win64\Godot_v4.6.2-stable_mono_win64.exe`

---

## Automated Tests

**Status**: PASS (115 C# test projects, 115 passing)

Commands:

```powershell
$projects = @(rg --files tests -g "*.csproj" | Sort-Object)
foreach ($p in $projects) { dotnet run --project $p -p:UseSharedCompilation=false }
dotnet build CloudWeaverVoyage.sln --no-restore -p:UseSharedCompilation=false
```

Results:

- Full test project sweep: 115/115 PASS
- `tests/csharp/FoundationParity/FoundationParity.csproj`: 70/70 PASS
- UI/HUD Story 001-006 runners: 134/134 PASS
- `tests/integration/session/ShellUiTest.csproj`: 18/18 PASS
- `dotnet build CloudWeaverVoyage.sln --no-restore -p:UseSharedCompilation=false`: PASS, 0 warnings, 0 errors
- `git diff --check`: PASS; only LF/CRLF conversion warnings reported

---

## Godot Runtime Smoke Probe

**Status**: PASS

Command:

```powershell
& 'D:\Program Files (x86)\Godot_v4.6.2-stable_mono_win64\Godot_v4.6.2-stable_mono_win64.exe' --headless --path 'D:\gameProjects\develop\Claude-Code-Game-Studios' -s 'tests/smoke/session_shell_visual_probe.gd'
```

Verified:

- `SessionShell` scene loads
- Entry and audio panels transition correctly
- Shell UI releases mouse input to Hub after gameplay starts
- `HubRuntime` mounts
- HUD / Chart entry is visible
- Save and Load entries are visible
- Save and Load actions show success feedback
- Chart panel opens and identifies the UI/HUD surface
- Hub Chart / Save / Load entries are disabled while the Chart panel is open
- Hub Chart / Save / Load entries leave the focus chain while the Chart panel is open
- Route departure closes Chart and opens the Exploration HUD bridge
- Exploration pressure loop mutates resource pressure, threat feedback, hull feedback, and recovery text over three advances
- Exploration state can be saved, restored, and returned to Hub
- Hub summary syncs exploration cargo, storage rewards, hull damage, and route pressure after return

Screenshot capture was skipped because the headless display driver cannot provide a runtime texture.

---

## Godot Numeric Performance Probe

**Status**: PASS

Command:

```powershell
& 'D:\Program Files (x86)\Godot_v4.6.2-stable_mono_win64\Godot_v4.6.2-stable_mono_win64.exe' --path 'D:\gameProjects\develop\Claude-Code-Game-Studios' -s 'tests/smoke/session_shell_perf_probe.gd'
```

Results:

- Windowed renderer: OpenGL API 3.3.0 NVIDIA 596.21, NVIDIA GeForce RTX 3070 Ti
- Frame samples: 356
- Frame time: avg 0.507ms, worst 3.980ms
- Peak static memory: 52.263MiB
- Peak draw calls: 103
- Boot to Hub: 75.655ms
- Chart open/close: avg 2.156ms, worst 3.368ms across 10 cycles
- Save p50/p95/max: 1.075/1.461/1.461ms across 10 cycles
- Load p50/p95/max: 0.959/1.469/1.469ms across 10 cycles
- Route departure: 4.554ms
- Return Hub: 3.202ms

All measured smoke-loop budgets passed.

---

## Test Coverage

| Story | Type | Test File | Coverage Status |
|-------|------|-----------|-----------------|
| Story 001: Screen State Machine & Screen Flow | Logic | `tests/unit/ui-hud-interface/ScreenStateMachineTest.csproj` | COVERED |
| Story 002: Modal Stack, Combat Override & Input Routing | Logic | `tests/unit/ui-hud-interface/ModalStackInputRoutingTest.csproj` | COVERED |
| Story 003: HUD Update, Panel Lifecycle & Cache | Logic | `tests/unit/ui-hud-interface/HudUpdatePanelLifecycleTest.csproj` | COVERED |
| Story 004: Upstream Data Contracts & Domain Integration | Integration | `tests/integration/ui-hud-interface/UpstreamDataContractsTest.csproj` | COVERED |
| Story 005: Animation Timing & Downstream Semantic Events | Integration | `tests/integration/ui-hud-interface/AnimationEventsTest.csproj` | COVERED |
| Story 006: Edge Cases, Desktop Recovery & Accessibility | Integration | `tests/integration/ui-hud-interface/EdgeCasesDesktopA11yTest.csproj` | COVERED |

**Summary**: 6 covered, 0 manual, 0 missing, 0 expected.

---

## Manual Smoke Checks

- [x] Batch 1: Core stability — PASS
  - Developer response: "正常"
  - Game launch, new session start, and main menu input response are normal.
- [x] Batch 2: Sprint mechanic and regression — PASS
  - Developer response: "UI/HUD 可见，航图可打开，底层入口不再抢焦点"
  - UI/HUD critical path is discoverable.
  - Chart can open.
  - Hub-level entries no longer steal focus while Chart is open.
- [x] Batch 3: Data integrity and performance — PASS
  - Developer response: "保存/加载入口可见，并显示保存完成/加载完成"
  - Save and Load entries are visible and return completion feedback.

---

## Missing Test Evidence

All UI/HUD Logic and Integration stories have test coverage.

---

## Verdict: PASS

Smoke check passed cleanly. The build is ready for manual QA.

QA hand-off: prepare the current UI/HUD sprint evidence for QA verification.
