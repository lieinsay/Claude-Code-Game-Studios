# 固定单位规格

固定单位默认不自行移动，位置通常由场景摆放决定。但“固定”不等于“背景”或“无状态”。树可以砍伐和再生，门可以开关，残骸可以被搜索，摊位可以随修复状态变化，灯塔可以从损坏变为点亮。

本目录存放 `fixed_scene_object` 规格。每个规格必须说明：

- 为什么它是固定单位，而不是实体单位。
- 是否会被破坏、采集、开关、修复、再生或切换状态。
- 状态变化是否影响碰撞、遮挡、交互、资源产出或场景身份。
- 玩家是否能走到它后方，是否需要 `behind_object_reveal`。
- UI 如何辅助说明，但不能替代该世界对象。
